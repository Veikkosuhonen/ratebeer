<%= form_with(model: beer) do |form| %>
  <% if beer.errors.any? %>
    <section style="color: red">
      <h2><%= pluralize(beer.errors.count, "error") %> prohibited this beer from being saved:</h2>
      <ul>
        <% beer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </section>
  <% end %>

  <section>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </section>

  <section>
    <%= form.label :style_id %>
    <%= form.collection_select :style_id, @styles, :id, :to_s %>
  </section>

  <section>
    <%= form.label :brewery_id %>
    <%= form.collection_select :brewery_id, @breweries, :id, :to_s %>
  </section>

  <section>
    <%= form.submit class: "btn-main" %>
  </section>
<% end %>
