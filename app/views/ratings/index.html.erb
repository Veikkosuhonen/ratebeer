<h2>List of ratings</h2>

<p><%= @ratings.count %> ratings</p>

<%= link_to "Create rating", new_rating_path if current_user %>

<ul>
  <% @ratings.each do |rating| %>
    <li>
      <%= render rating %>
    </li>
  <% end %>
</ul>
